extends layout/main.pug
include mixins/videoBlock

block content
    .videos
        h1 Video
        each item in videosis
            +videoBlock({
                id:item.id,
                title: item.title,
                views: item.views,
                videoFIle: item.videoFile,
            })